# PostgreSQL connection string
DATABASE_URL=postgres://user:password@localhost:5432/discography

# Used by tests — point at a separate test database
DATABASE_URL_TEST=postgres://user:password@localhost:5432/discography_test

# Port the API listens on (default: 3000)
PORT=3000
